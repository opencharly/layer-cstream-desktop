# 2026.242.0814 — feat: advance both members to the fixed tags

## Summary

Advance both members to the tags that actually work:

| member | from | to | why |
|---|---|---|---|
| `pod-cstream` | `v2026.242.0352` | `v2026.242.0658` | the older tag pinned a `charly-streamer` commit predating the in-process signalling server, so its streamer reached PLAYING and then **died on the bus**; the new tag also resolves `pod-hyprland` at one version instead of two |
| `pod-hyprland` | `v2026.241.1545` | `v2026.242.0634` | carries `plugin-wl v2026.242.0610`, where `wl: close` and `wl: fullscreen` stopped acting on the **focused** window instead of the named one |

**Composing the old tags made charly resolve two versions and silently pick the newest:**

```
Warning: candy github.com/opencharly/pod-hyprland resolved to multiple versions; using newest …, ignoring …
Warning: candy github.com/opencharly/plugin-wl/candy/plugin-wl resolved to multiple versions; using newest …, ignoring …
```

Both go to zero here. Taking the newest happened to be right, but it was charly's tie-break rather
than something this candy declared.

## Change classification

**Change class:** candy/box-config — two pinned tags, one candy `version:`. No Go, no schema, no
workflow, single repo.

**Verification gate:** `check-cstream-desktop`, a `disposable: true` bed that composes **this
metalayer and nothing else**, with no repo overrides — every candy from a published tag.

**Breaking change + rollback:** none in shape. The behavioural consequence is that the streamer can
serve and the `wl:` window verbs aim correctly. Rollback is a plain revert and restores both bugs.

**Producer → consumer order:** satisfied — both tags are merged and published.

## How tested

**1. The version-skew warnings, before and after:**

```
before:  charly box validate  →  2 warnings (pod-hyprland, plugin-wl: multiple versions)
after:   charly box validate  →  0 warnings
```

**2. The bed — composing only this metalayer, with NO overrides:**

```
$ charly check run check-cstream-desktop        # no repo overrides
[image-build] PASS after 9m24.989s [cache=33/72]   [check-image] PASS after 24m30.949s
[deploy-add] PASS after 17.742s                    [config] PASS after 5.356s
[start] PASS after 5.261s                          [bring-up-members] PASS
[check-live] PASS after 2m9.377s                   [feature-run] PASS after 1m11.392s
[update] PASS after 28.112s                        [check-live-rebuild] PASS after 4m1.582s
[feature-run-rebuild] PASS after 2m0.55s           [cleanup] PASS after 5m13.581s
charly check run check-cstream-desktop: PASS (steps=13)

live checks: 48 PASS / 0 FAIL   (and 48/0 again on the rebuild leg)
summed over every step:  errors=0  warnings=0  allowlisted=30
```

The long `check-image` is host-side, not this candy: the workstation's image store holds ~10.8k
images and charly's unfiltered `podman images --format json` is O(store) — filed as
`opencharly/charly#475`. The step passed.

Everything the bed exercises arrives *through* this candy: the GWD DMABuf negotiation, the `wl:`
verb against the live nested compositor, the Hyprland config set, gateway readiness, the two-way
streaming gate, hardware encode, the streamer's own graph **and** its serving path, and both PAM
login legs.

**3. The checks that would fail at the OLD member tags:**

```
PASS  check the streamer SERVES -- the bus loop runs and the sink registers as a producer  [cstream-streamer-serves]
PASS  check the wl verb closes the NAMED window, not the focused one                       [wl-close-target]
PASS  check the named window is gone AND the focused one survived                          [wl-close-observed]
```

The first fails at the old `pod-cstream` (its streamer dies on the bus); the last two fail at the
old `pod-hyprland` (its `plugin-wl` closes the focused window). So this is not a cosmetic advance —
each pin move is load-bearing and is covered by a check that discriminates.

## Rulebook compliance

- **R0 — skills honoured.** `/charly-internals:git-workflow` for the pinning policy.
- **R1 — RCA every anomaly.** The two resolution warnings were traced to the member/candy pin skew
  and driven to zero rather than suppressed; the run is at `errors=0 warnings=0`.
- **R2 — nothing parked.** Both members move together, so no third version remains in the graph.
- **R3 — no duplication.** Two pins, one place each.
- **R4 — no workaround.** Published tags; no reliance on charly's newest-wins tie-break.
- **R5 — delete legacy completely.** `N/A` — value advances; the old tags have no other occurrence.
- **R6 — git safety.** `git status` before staging; `feat/` branch off a fresh clone of `main`; no
  force-push; no direct push to `main`.
- **R7/R10 — prove the gate on the final committed tree.** The bed ran against this tree with no
  overrides, is `disposable: true`, and completed its destroy-and-rebuild leg.

## Attribution tier

**fully tested and validated** — a bed composing only this metalayer is green with every candy
resolved from its published tag, at `errors=0 warnings=0`, through the destroy-and-rebuild leg.

---

*Assisted-by: Claude Code claude-opus-5[1m] (fully tested and validated)*