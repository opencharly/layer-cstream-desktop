# 2026.243.0607 — chore: advance pod-hyprland so the chain reaches one plugin-wl version

## Summary
    require:  @github.com/opencharly/pod-hyprland
              v2026.243.0216 -> v2026.243.0603

Second of three ordered repins closing a plugin-wl version split across the org. This candy is
the middle link:

    distro-omarchy/box/omarchy-cstream -> layer-cstream-desktop  (THIS repo)
                                       -> pod-hyprland
                                       -> plugin-wl

pod-hyprland#8 advanced the tail from plugin-wl v2026.242.0610 to v2026.242.2204 (the first tag
carrying `wl: hypr-layers`). Until this pin moves, that fix reaches nobody: a consumer resolving
this candy still gets the older pod-hyprland and therefore the older plugin-wl.

That is the whole reason the split existed — a repin one link deep leaves every link above it
on the old version, which is how two versions ended up in one closure and made charly warn:

    Warning: candy github.com/opencharly/plugin-wl/candy/plugin-wl resolved to multiple
    versions; using newest ... ignoring 2026.187.1200 (from ...@v2026.240.1326)

## How tested

    $ charly box validate
    Downloading github.com/opencharly/pod-hyprland:v2026.243.0603...
    [exit 0]

The pin resolves and the chain it feeds now terminates at a single plugin-wl version, verified
by reading the newly pinned ref's own manifest rather than assuming the bump landed:

    plugin-wl/candy/plugin-wl:v2026.242.2204

This repo declares one candy and no disposable bed, so charly box validate is its gate. The
end-to-end proof belongs to the consumer that closes the chain, opencharly/distro-omarchy#11,
whose bed re-runs with the warning count at zero.

## Rulebook compliance

- **R1 — RCA every anomaly.** The multi-version warning was traced link by link to its source
  and is being fixed at each link, not suppressed or allowlisted at the consumer.
- **R2 — Nothing parked.** Second of three ordered repins; the first (pod-hyprland#8) is merged
  and tagged, the third is opencharly/distro-omarchy#11.
- **R3 — No duplication.** One shared pin advanced; no per-consumer override introduced.
- **R4 — No workarounds.** The version split is closed at its cause rather than masked.
- **R6 — Git safety.** Branched from a fresh `origin/main`; no force-push, no hook bypass, no
  direct push to main.
- **R7 — Prove the gate, not the plan.** `charly box validate` pasted, and the newly pinned
  ref's own manifest read back to confirm the chain terminates at one plugin-wl version.

## Change classification

| | |
|---|---|
| **Change class** | pin advance (one `require:` ref + the candy version) |
| **Verification gate** | `charly box validate` — this repo declares one candy and no `disposable: true` bed |
| **Attribution tier** | `analysed on a live system` — validate passed and the resolved chain was read back live; no bed exists in THIS repo, and the end-to-end bed proof belongs to the consumer closing the chain |

*Assisted-by: Claude Code claude-opus-4-6-20260401 (analysed on a live system)*